# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
POST        /                   controllers.PolygonService.post
POST        /offset_poly        controllers.PolygonService.post
